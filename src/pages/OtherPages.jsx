/* PacksPage.jsx */
export function PacksPage() {
  /* same product grid as homepage but with filter bar + page header */
}

/* SizeGuidePage.jsx */
export function SizeGuidePage() {
  /* size table + how to measure card + CTA buttons */
}

/* ReferralPage.jsx */
export function ReferralPage() {
  /* Rs.200 reward card + 3-step how it works + CTA */
}

/* CartPage.jsx */
export function CartPage() {
  /* 2-col layout: cart items + upsell + order summary */
}
